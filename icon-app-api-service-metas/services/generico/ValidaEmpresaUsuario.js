const sequelize = require('sequelize');

/**
 * Valida se o usuário que está executando a ação
 * é participante ativo da empresa :)
 */
module.exports.validaEmpresaUsuario = (empresa, Usuario) => {
    console.info('AGORA TEREMOS VALIDAÇÃO OK? kkkk')
    return true
}
